name: Android Build

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Test Environment
      run: echo "Hello World - Environment is working"

    - name: List Root Files
      run: ls -la

    - name: List Android Directory
      run: ls -la android/
